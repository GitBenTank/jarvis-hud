# Social Copy for Jarvis HUD

**Repo:** https://github.com/GitBenTank/jarvis-hud

Copy the blocks below into **GitHub → Settings → General** (Description + Topics). Wording matches **Thesis Lock**: agents propose, humans approve, execution is separate, every action leaves proof.

---

## GitHub Repo Settings

**Description** (Settings → General → Description):

> Jarvis HUD — Control plane for local agents: agents propose, humans approve, execution is a separate step, every action leaves proof (receipt + artifact + trace). OpenClaw ingress optional. Demo-ready; see README.

**Topics** (Settings → General → Topics):

```
ai-agents
ai-governance
automation
control-plane
developer-tools
ai-safety
openclaw
agent-frameworks
```

**Short taglines** (for search/profile):

- *A control plane for AI agents. Proposals, approvals, execution, and receipts.*
- *GitHub-style approvals for AI agents. Propose → approve → execute → trace.*

---

## Pinned Repo Description (Profile)

When pinning Jarvis HUD to your GitHub profile, use one of these:

**Recommended (thesis-aligned):**
> Jarvis HUD — Agents propose, humans approve, execution is separate, every action leaves proof. Local-first control plane for governed agent automation.

**Shorter:**
> Jarvis HUD — A control plane for AI agents. Propose → approve → execute → trace.

**Devtool style:**
> Jarvis HUD — GitHub-style approvals for AI agents. Proposals, human gates, execution receipts, and traceable automation.

---

## Profile Headline (optional)

> Building AI tools and control planes for safe automation. Creator of Jarvis HUD.

Or:

> Developer building AI tools, control planes, and weird experiments.

---

## Twitter / LinkedIn one-liner

**Full:**
> I built Jarvis HUD, a control plane for AI agents. Agents propose actions → humans approve them → the system executes them with receipts and a traceable history.

**Short:**
> Jarvis HUD: GitHub-style approvals for AI agents.

---

## LinkedIn Post

**Secure AI code execution — open source**

AI agents can write files, run tools, and call APIs. The gap isn't intelligence. It's control.

I built Jarvis HUD: a local-first control plane that enforces:

→ AI proposes  
→ Humans authorize  
→ Every action produces receipts

It's not another agent. It's the approval layer that makes AI coding tools safe for teams.

Built with Next.js, fully open source. Check it out:

https://github.com/GitBenTank/jarvis-hud

---

## Facebook Post

Built something for developers who use AI coding tools (Cursor, Claude Dev, etc.) and want to stay in control.

Jarvis HUD = AI proposes, humans approve, every action has receipts.

No silent runs. No blind trust. Open source.

https://github.com/GitBenTank/jarvis-hud

---

## YouTube Pinned Comment Template

**Jarvis HUD — Secure AI Code Execution**

Repo: https://github.com/GitBenTank/jarvis-hud

What it does:
• Control plane for AI agents with file/tool access
• Propose → Approve → Execute (approval ≠ execution)
• Receipts for every action (artifact + log)
• Optional auth + step-up for execute
• Adapters: code.diff, content.publish, youtube.package, system.note

Stack: Next.js 16, React 19, TypeScript, pnpm

Dev: `pnpm dev` → http://127.0.0.1:3000 · Demo boot: `pnpm demo:boot` → http://127.0.0.1:3001 (see README)
